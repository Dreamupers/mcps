[project]
name = "mcps"
version = "0.1.0"
description = "MCP servers"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "requests>=2.31.0",
    "markdown-it-py",
    "mdit-py-plugins",
]
